general {
  lock_cmd = hyprlock
  before_sleep_cmd = hyprlock
}

listener {
  timeout = 600          # 10 min
  on-timeout = hyprlock  # lock
}

listener {
  timeout = 800
  on-timeout = hyprctl dispatch dpms off
  on-resume = hyprctl dispatch dpms on
}
